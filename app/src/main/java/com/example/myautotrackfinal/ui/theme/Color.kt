package com.example.myautotrackfinal.ui.theme

import androidx.compose.ui.graphics.Color

// Colores para el tema oscuro (DarkColorScheme)
// Estos se usarán como primary, secondary, tertiary en el modo oscuro.
// He elegido un azul más claro y un rosa vibrante que contrastan bien con fondos oscuros.
val DarkPrimary = Color(0xFF6EC6FF) // Un azul claro y vibrante (antes Purple80)
val DarkSecondary = Color(0xFFFF80AB) // Un rosa brillante (antes PurpleGrey80)
val DarkTertiary = Color(0xFFF06292) // Una variante de rosa un poco más suave (antes Pink80)

// Colores para el tema claro (LightColorScheme)
// Estos se usarán como primary, secondary, tertiary en el modo claro.
// He elegido un azul más oscuro y un rosa más profundo para un aspecto moderno y fuerte.
val LightPrimary = Color(0xFF2962FF) // Un azul intenso (antes Purple40)
val LightSecondary = Color(0xFFD500F9) // Un fucsia para contraste (antes PurpleGrey40)
val LightTertiary = Color(0xFF7B1FA2) // Una variante más oscura para el fucsia (antes Pink40)

// Colores básicos que puedes mantener para texto o fondos
val Black = Color(0xFF000000)
val White = Color(0xFFFFFFFF)

// Puedes añadir más colores si los necesitas para elementos específicos,
// por ejemplo, para variantes de los colores primarios/secundarios
// val BlueVibrant = Color(0xFF2962FF)
// val BlueDarker = Color(0xFF0039CB)
// val PinkBright = Color(0xFFFF4081)
// val PinkDarker = Color(0xF50057)