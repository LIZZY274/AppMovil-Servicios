package com.example.myautotrackfinal.features.home.data

// No se requiere interacción directa con la API para esta pantalla simple.
// Si tuvieras datos que cargar en el home (ej. saldo total), irían aquí.
class HomeRepository {
    // Ejemplo de método dummy si necesitaras algo en el futuro
    // suspend fun getDashboardStats(): DashboardStats { ... }
}